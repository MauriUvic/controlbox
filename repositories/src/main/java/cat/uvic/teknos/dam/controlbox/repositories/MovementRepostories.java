package cat.uvic.teknos.dam.controlbox.repositories;



public interface MovementRepostories {
    void insert(Movement movement);
    void update(Movement movement);
    void delete(Movement movement);
}